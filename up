云推有新版时客户端会自动更新，但是由于机器本身卡顿的原因，会有部分机器更新不成功，这个时候会发送更新失败通知到管理者微信，然后可以按照下面的方法手动更新。

云推手动更新方法

1、强制退出正在执行的应用【没运行则忽略，保证升级时不在运行，否则手动更新不成功】

2、复制下面的命令到黑色的命令行执行【会自动下载应用和CPushRest到应用程序中】

sudo rm -rf /Applications/CloudPush*.app && sudo rm -rf /Applications/CPushReset.app && rm -rf ~/.CloudPush/*.sh && rm -rf ~/.CloudPush/cache/.subAction && rm -rf ~/.CloudPush/cache/.actionExpire && rm -rf ~/.CloudPush/cache/.reRunApp && rm -rf ~/.CloudPush/cache/.updateApp && ls | grep cloudpush | xargs launchctl unload -w && ls | grep cloudpush | xargs sudo rm -rf && cd ~/Downloads && curl --output CloudPush.zip http://107.165.166.2/app/download && unzip CloudPush.zip && sudo mv CloudPush*.app /Applications


3、打开“系统偏好设置->安全与隐私->隐私”,把CloudPushVXX和CPushRest加入到辅助功能列表

4、双击应用打开即可
